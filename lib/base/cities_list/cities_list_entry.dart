/// Simple data class for wrapping city name and it's countryCode
class CitiesListEntry {
  final String cityName;
  final int countryCode;

  const CitiesListEntry(this.cityName, this.countryCode);
}
