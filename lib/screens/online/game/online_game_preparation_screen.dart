import 'package:flutter/material.dart';

class OnlineGamePreparationScreen extends StatelessWidget {
  const OnlineGamePreparationScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
